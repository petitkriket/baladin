<p><%= t('.welcome_user', name: @resource.name) %></p>

<p><%= t('.request') %></p>

<p><%= link_to t('.change'), edit_password_url(@resource, reset_password_token: @token) %></p>

<p><%= t('.notme') %></p>
<p><%= t('.nochange') %></p>
