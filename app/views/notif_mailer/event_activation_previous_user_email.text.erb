<%= t('.greeting', name: @user.name) %>

<%= t('.participation', passenger_name: @passenger.name, name: @passenger.events.last.user.name) %>
<%= t('.view') %><%= t('.link') %>:
    <%= "https://#{Rails.application.credentials.dig(:domain_name)}/passenger/#{@event.passenger_id}" %>
